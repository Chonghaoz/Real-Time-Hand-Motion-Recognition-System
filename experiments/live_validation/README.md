# Live Validation Artifacts

These files are retained as evidence of deployment debugging. The CSV files contain feature vectors captured from the ESP32 during real-world motion tests. The Python scripts were used to replay exported tree inference and compare embedded behavior with Python-side predictions.

They are experimental/debug artifacts rather than the primary training pipeline.
